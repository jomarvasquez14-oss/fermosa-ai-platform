# dependencies
/node_modules
.pnpm-store/

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# prisma
/prisma/migrations/dev.db*

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Local storage provider root (binary uploads)
.storage/

# Real logbook samples + calibration outputs (patient PII — never commit)
ocr-samples/

# Authoritative CRM reference captures (real patient/staff PII — local only,
# PROJECT_RULES 24). Ask the project lead before ever committing these.
docs/crm-reference/

# Generated CRM dataset output (M0045) — reproducible from the connector on
# demand; never committed.
datasets/
